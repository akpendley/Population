/// @description start dragging

if (!global.dragging)
{
	// if the singleton dragging is not taken
	global.dragging = true;
	// take it and
	dragging = true;
	// tell this human they are the singleton
}